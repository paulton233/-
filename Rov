<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ROV Topup Store | ชำระเงินผ่าน QR Code</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Kanit', sans-serif; background-color: #0f172a; }
        .card-grad { background: linear-gradient(145deg, #1e293b, #0f172a); }
        .gold-text { color: #fbbf24; }
        .modal { display: none; position: fixed; z-index: 100; left: 0; top: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); }
    </style>
</head>
<body class="text-gray-200">

    <nav class="p-4 border-b border-slate-700 bg-slate-900 sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold gold-text italic">ROV STORE</h1>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-10">
        <div class="max-w-4xl mx-auto">
            <div class="card-grad p-6 rounded-2xl border border-slate-700 mb-6">
                <h2 class="text-xl font-semibold mb-4 italic">1. ข้อมูลผู้เล่น</h2>
                <input type="text" id="playerId" placeholder="กรอก Player ID" class="w-full bg-slate-800 border border-slate-600 p-4 rounded-xl outline-none focus:ring-2 focus:ring-yellow-500">
            </div>

            <div class="card-grad p-6 rounded-2xl border border-slate-700 mb-6">
                <h2 class="text-xl font-semibold mb-6 italic">2. เลือกแพ็กเกจ</h2>
                <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
                    <div onclick="selectPkg(15, '20 คูปอง')" class="pkg border-2 border-slate-600 p-4 rounded-xl cursor-pointer hover:border-yellow-500">
                        <p class="text-sm">20 คูปอง</p>
                        <p class="text-xl font-bold gold-text">฿15</p>
                    </div>
                    <div onclick="selectPkg(75, '100 คูปอง')" class="pkg border-2 border-slate-600 p-4 rounded-xl cursor-pointer hover:border-yellow-500">
                        <p class="text-sm">100 คูปอง</p>
                        <p class="text-xl font-bold gold-text">฿75</p>
                    </div>
                </div>
            </div>

            <div id="payBtnArea" class="hidden">
                <button onclick="openModal()" class="w-full bg-green-500 hover:bg-green-600 text-white font-bold py-4 rounded-xl text-lg shadow-lg transition">
                    ยืนยันคำสั่งซื้อและรับ QR Code
                </button>
            </div>
        </div>
    </div>

    <div id="paymentModal" class="modal flex items-center justify-center p-4">
        <div class="bg-white text-black p-6 rounded-3xl max-w-sm w-full text-center relative">
            <button onclick="closeModal()" class="absolute top-4 right-4 text-gray-400 hover:text-black text-2xl">&times;</button>
            
            <h3 class="text-xl font-bold mb-2">สแกนเพื่อจ่ายเงิน</h3>
            <p class="text-sm text-gray-500 mb-4">ชื่อบัญชี: ด.ช. วรธวีร์ สมิตะเศรษฐ์</p>
            
            <div class="bg-gray-100 p-4 rounded-2xl mb-4">
                <img src="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=PROMPTPAY_DATA_HERE" alt="QR Payment" id="qrImg" class="w-full h-auto">
                </div>

            <div class="bg-blue-50 p-3 rounded-xl mb-4 text-left border border-blue-100">
                <p class="text-xs text-blue-800">ยอดที่ต้องโอน:</p>
                <p id="modalPrice" class="text-2xl font-bold text-blue-900 italic">฿0</p>
            </div>

            <p class="text-[10px] text-red-500">*เมื่อโอนเสร็จแล้ว กรุณาส่งสลิปยืนยันในแอดมิน</p>
        </div>
    </div>

    <script>
        let currentPrice = 0;
        let currentName = "";

        function selectPkg(price, name) {
            currentPrice = price;
            currentName = name;
            document.querySelectorAll('.pkg').forEach(el => el.classList.remove('border-yellow-500', 'bg-slate-800'));
            event.currentTarget.classList.add('border-yellow-500', 'bg-slate-800');
            document.getElementById('payBtnArea').classList.remove('hidden');
        }

        function openModal() {
            if(!document.getElementById('playerId').value) return alert("กรุณาใส่ ID ผู้เล่น");
            document.getElementById('modalPrice').innerText = "฿" + currentPrice;
            document.getElementById('paymentModal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('paymentModal').style.display = 'none';
        }
    </script>
</body>
</html>
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ROV Topup Store | ชำระเงินผ่าน QR Code</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Kanit', sans-serif; background-color: #0f172a; }
        .card-grad { background: linear-gradient(145deg, #1e293b, #0f172a); }
        .gold-text { color: #fbbf24; }
        .modal { display: none; position: fixed; z-index: 100; left: 0; top: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); }
    </style>
</head>
<body class="text-gray-200">

    <nav class="p-4 border-b border-slate-700 bg-slate-900 sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold gold-text italic">ROV STORE</h1>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-10">
        <div class="max-w-4xl mx-auto">
            <div class="card-grad p-6 rounded-2xl border border-slate-700 mb-6">
                <h2 class="text-xl font-semibold mb-4 italic">1. ข้อมูลผู้เล่น</h2>
                <input type="text" id="playerId" placeholder="กรอก Player ID" class="w-full bg-slate-800 border border-slate-600 p-4 rounded-xl outline-none focus:ring-2 focus:ring-yellow-500">
            </div>

            <div class="card-grad p-6 rounded-2xl border border-slate-700 mb-6">
                <h2 class="text-xl font-semibold mb-6 italic">2. เลือกแพ็กเกจ</h2>
                <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
                    <div onclick="selectPkg(15, '20 คูปอง')" class="pkg border-2 border-slate-600 p-4 rounded-xl cursor-pointer hover:border-yellow-500">
                        <p class="text-sm">20 คูปอง</p>
                        <p class="text-xl font-bold gold-text">฿15</p>
                    </div>
                    <div onclick="selectPkg(75, '100 คูปอง')" class="pkg border-2 border-slate-600 p-4 rounded-xl cursor-pointer hover:border-yellow-500">
                        <p class="text-sm">100 คูปอง</p>
                        <p class="text-xl font-bold gold-text">฿75</p>
                    </div>
                </div>
            </div>

            <div id="payBtnArea" class="hidden">
                <button onclick="openModal()" class="w-full bg-green-500 hover:bg-green-600 text-white font-bold py-4 rounded-xl text-lg shadow-lg transition">
                    ยืนยันคำสั่งซื้อและรับ QR Code
                </button>
            </div>
        </div>
    </div>

    <div id="paymentModal" class="modal flex items-center justify-center p-4">
        <div class="bg-white text-black p-6 rounded-3xl max-w-sm w-full text-center relative">
            <button onclick="closeModal()" class="absolute top-4 right-4 text-gray-400 hover:text-black text-2xl">&times;</button>
            
            <h3 class="text-xl font-bold mb-2">สแกนเพื่อจ่ายเงิน</h3>
            <p class="text-sm text-gray-500 mb-4">ชื่อบัญชี: ด.ช. วรธวีร์ สมิตะเศรษฐ์</p>
            
            <div class="bg-gray-100 p-4 rounded-2xl mb-4">
                <img src="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=PROMPTPAY_DATA_HERE" alt="QR Payment" id="qrImg" class="w-full h-auto">
                </div>

            <div class="bg-blue-50 p-3 rounded-xl mb-4 text-left border border-blue-100">
                <p class="text-xs text-blue-800">ยอดที่ต้องโอน:</p>
                <p id="modalPrice" class="text-2xl font-bold text-blue-900 italic">฿0</p>
            </div>

            <p class="text-[10px] text-red-500">*เมื่อโอนเสร็จแล้ว กรุณาส่งสลิปยืนยันในแอดมิน</p>
        </div>
    </div>

    <script>
        let currentPrice = 0;
        let currentName = "";

        function selectPkg(price, name) {
            currentPrice = price;
            currentName = name;
            document.querySelectorAll('.pkg').forEach(el => el.classList.remove('border-yellow-500', 'bg-slate-800'));
            event.currentTarget.classList.add('border-yellow-500', 'bg-slate-800');
            document.getElementById('payBtnArea').classList.remove('hidden');
        }

        function openModal() {
            if(!document.getElementById('playerId').value) return alert("กรุณาใส่ ID ผู้เล่น");
            document.getElementById('modalPrice').innerText = "฿" + currentPrice;
            document.getElementById('paymentModal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('paymentModal').style.display = 'none';
        }
    </script>
</body>
</html>
